apiVersion: v1
kind: Service
metadata:
  name: orion-platform-orders-svc
  labels:
    app.kubernetes.io/name: orion-platform
    app.kubernetes.io/component: orders
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: orion-platform
    app.kubernetes.io/component: orders
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
